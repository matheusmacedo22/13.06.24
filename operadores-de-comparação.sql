-- Selecionar apenas o `id` da tabela cidades: ???

-- Operadores de comparação do SQL:
-- "=" comparação idêntica
-- "LIKE" comparação semelhante
-- "%" dentro da string, serve como "qualquer caracter" antes, durante ou depois na string

SELECT * FROM `db_sala_16`.`cidades` WHERE `nome` = 'londrina';